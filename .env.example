# =============================================================================
# GCP インフラ設定（.env.example）
#
# このファイルを .env にコピーして、実際の値を入力してください。
#   cp .env.example .env
#
# .env ファイルは .gitignore に含まれており、リポジトリにコミットされません。
# =============================================================================

# GCP プロジェクト ID（必須）
GCP_PROJECT_ID=your-gcp-project-id

# GCP リージョン（デフォルト: asia-northeast1）
GCP_REGION=asia-northeast1

# Cloud Run サービス名（デフォルト: reservation-timeslot-checker-unified）
CLOUD_RUN_SERVICE_NAME=reservation-timeslot-checker-unified

# Cloud Run URL（省略時はスクリプトが自動取得）
# CLOUD_RUN_URL=https://your-service-xxxxx.asia-northeast1.run.app

# Cloud Storage バケット名
BUCKET_NAME=your-bucket-name
